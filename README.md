Note: Before build, don't forget to increment project version if needed.

=== Build npm package

```
cd <project dir>
npm run build
```

=== Publish npm package

```
cd <project dir>
npm run publish
```